#include "find_root.h"
#include <functional>

int find_root(std::function<double(double)> f) {
    // implement your function here
    double e = 0, ne = e;

    while (f(e)) {


    }
    return 0;
}
