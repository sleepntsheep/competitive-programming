#include <stdio.h>
#define mxn 20005

int a[mxn];
int mx[mxn];
int mn[mxn];
int main() {
    int n, x, y;
    scanf("%d%d%d", &n, &x, &y);
    for (int i = 0; i < n; i++)
        scanf("%d", a+i);

}
