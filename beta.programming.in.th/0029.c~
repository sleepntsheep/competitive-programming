#include <stdio.h>

int main() {
	int x, y;
	scanf("%d%d", &x, &y);
	//if (y < x)
	//	y = x;
	//if (y % x > 0)
	//	y += x;
	//printf("%d", y / x);
	printf("%d", y / x + (y % x > 0) + (y < x));

}
