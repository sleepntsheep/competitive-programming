#include <bits/stdc++.h>
using namespace std;

// swap 
// A B C --  C B A

int n, sum;
int A[505][505];

int main() {
    cin.tie(NULL);
    ios::sync_with_stdio(false);
    cin >> n;
    for (int i = 0; i < n; i++)
    for (int j = 0; j < n; j++)
        cin >> A[i][j];

    return 0;
}
