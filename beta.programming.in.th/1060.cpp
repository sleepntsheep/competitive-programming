#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    int a[n][2];
    int mh = - 1;
    for (int i = 0; i < n; i++){
        cin >> a[i][0] >> a[i][1];
        if (mh < a[i][1]) mh = a[i][1];
    }

    for (int i =0 ;i < mh; i++){
        for (int j = 0; j < )
    }

    return 0;
}
