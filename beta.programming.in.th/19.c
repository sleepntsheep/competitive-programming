#include <stdio.h>

int main() {
  int n;
  scanf("%d", &n);
  int x[n];
  int y[n];
  for (int i = 0; i < n; i++) {
    int a, b;
    scanf("%d%d", &a, &b);
    x[i] = a;
    y[i] = b;
  }
  int min = 2000000000;

}
