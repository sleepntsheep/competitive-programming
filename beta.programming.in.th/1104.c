#include <stdio.h>

int main() {
    int n, m;
    int A[1000], B[1000], C[100];
    scanf("%d%d", &n, &m);
    for (int i = 0; i < n; i++)
        scanf("%d", &C[i]);
    for (int i = 0; i < m; i++)
        scanf("%d%d", &A[i], &B[i]);



}
