#include <stdio.h>

int main() {
  int n;
  scanf("%d", &n);
  int all[n];

  for (int i = 0; i < n; i++){
    char s[];
    char ch;
    ch = getchar();
    while (ch != EOF && ch != '\n'){
      
    }
    all[i] = z;
  }
}
