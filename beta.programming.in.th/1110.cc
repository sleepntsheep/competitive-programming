#include <bits/stdc++.h>

using namespace std;


int cs[1000010];

int n, k;
int main() {
    cin >> n >> k;
    vector<int> v;
    for (int i =0, a;i  < n; i ++) {
        cin >> a;
        v.push_back(a);
    }

}
