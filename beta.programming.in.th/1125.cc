#include <bits/stdc++.h>
using namespace std;
typedef uint64_t ll;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    ll sum = 0;


    cout << sum;
    return 0;
}
